# Docker Compose Environment Variables for Unraid
# Copy this to .env in your project root for Docker deployment

# PostgreSQL Database Password
POSTGRES_PASSWORD=your_secure_database_password_here

# Plex Configuration
PLEX_URL=http://your-unraid-ip:32400
PLEX_TOKEN=your-plex-token-here

# API Keys (optional but recommended)
TVDB_API_KEY=your-tvdb-api-key
COMICVINE_API_KEY=your-comicvine-api-key

# Additional Settings
JWT_SECRET=your-jwt-secret-for-production

# Timezone Configuration
# Set your timezone offset in hours from UTC
# Examples: -5 for EST, -8 for PST, -6 for CST, 0 for UTC
# This fixes the "Today's Activity" showing wrong dates
TIMEZONE_OFFSET=-5
